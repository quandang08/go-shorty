# ================================
# App Configuration
# ================================
PORT=8080
SHORT_DOMAIN=http://localhost:8080/

# ================================
# PostgreSQL Configuration (Choose one)
# ================================

# --- OPTION 1: Single connection string (recommended for cloud deployment) ---
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=disable
# DATABASE_URL=postgres://shorty_user:shorty_pass@localhost:5432/shorty_db?sslmode=disable


# --- OPTION 2: Separate environment variables (common for local/Docker setups) ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=shorty_user
DB_PASSWORD=shorty_pass
DB_NAME=shorty_db
